from math import *
# from cmath import sqrt

print(sqrt(1))

"""
    若引入了一整个模块，那么必须在所有的函数前面都加上模块的名字
    即使没有重名函数
    若仅引入了一个模块中的一个库函数/使用了 from xxx import *，那么一定不能加模块名字
    若有重名函数，那么可能会发生意向的不到的覆盖
"""